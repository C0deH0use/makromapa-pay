info:
  app:
    host: http://localhost:9090

makromapa-auth:
  root:
    url: http://localhost:9091


spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/makromapa-db-auth
  flyway:
    locations: classpath:db/migration/default, classpath:db/migration/local, classpath:db/migration/test-data

security:
  oauth2:
    client:
      client-secret: secret

      spring.datasource.url=jdbc:postgresql://localhost:5433/makromapa-db-auth

      spring.flyway.locations=classpath:db/migration/default, classpath:db/migration/local, classpath:db/migration/test-data